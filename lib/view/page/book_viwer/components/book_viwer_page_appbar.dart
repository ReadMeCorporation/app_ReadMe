import 'package:flutter/material.dart';

class BookViwerPageAppbar extends StatelessWidget {
  const BookViwerPageAppbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
